import type { RotationPresetDocument } from '../domain/rotation-presets'

/** Reviewed community presets are promoted here without changing the import format. */
export const bundledRotationPresets: RotationPresetDocument[] = []
